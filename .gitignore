*.csv
!tests/expected/**/*.csv
*.sqlite
files_comp_results.txt
paramNotWritten.dat
.spinetoolbox/local/
.spinetoolbox/items/flextool/output/
.spinetoolbox/items/exporttocsv/exported.json
.spinetoolbox/items/
results/
*.bak*
*.log
*.mps
*.sol
# Live HiGHS options — user-editable; seeded from the bundled template on first run
solver_config/highs.opt
__pycache__
# Python build artifacts — setuptools regenerates these on every
# editable install / build / sdist; never repo assets.
*.egg-info/
build/
dist/
.idea/
.claude/
site/
example_input.xlsx
*.xls*
*.ods
# Spine Toolbox workflow runtime dirs — created on demand by the
# workflow when it runs in the project root. Not repo assets.
input/
output/
output_csv/
output_excel/
output_parquet/
output_plots/
ouptut_plot_comparisons/
solve_data/
# Templates regenerated by `flextool-update` from canonical JSON.
templates/
# Developer-only LP-scaling benchmark harness; kept locally, not shipped.
benchmarks/
flextool-venv/
.venv/
specs/
projects/
tests/unitTests.txt
rivendell/
.aider*
# Local audit / investigation handoffs — meant for personal note-taking,
# not shipped with the repo.  Files already tracked here predate this
# rule and stay tracked; new files in this folder are local-only.
_audit_reports/
_diagrams/
