*.csv
!tests/expected/**/*.csv
*.sqlite
# Results-DB sidecars: atomic-build temp shards and the cold-start lock file.
*.sqlite.tmp
.results-*.sqlite.tmp
*.sqlite.lock
files_comp_results.txt
paramNotWritten.dat
.spinetoolbox/local/
.spinetoolbox/items/flextool/output/
.spinetoolbox/items/exporttocsv/exported.json
.spinetoolbox/items/
results/
*.bak*
*.log
*.mps
*.sol
# Live solver options (HiGHS + commercial solvers) — user-editable;
# seeded from the bundled *.opt.template files on first run.
solver_config/*.opt
__pycache__
# Python build artifacts — setuptools regenerates these on every
# editable install / build / sdist; never repo assets.
*.egg-info/
build/
dist/
.idea/
.claude/
site/
example_input.xlsx
*.xls*
*.ods
# Spine Toolbox workflow runtime dirs — created on demand by the
# workflow when it runs in the project root. Not repo assets.
input/
output/
output_csv/
output_excel/
output_parquet/
output_parquet_comparison/
output_plots/
output_plot_comparisons/
output_raw/
solve_data/
# Templates regenerated by `flextool-update` from canonical JSON.
templates/
# Developer-only LP-scaling benchmark harness; kept locally, not shipped.
benchmarks/
flextool-venv/
.venv/
venv/
specs/
projects/
tests/unitTests.txt
# Local-only tests that read PROPRIETARY project datasets (H2/SouthAfrica/
# Cyprus) from the gitignored `projects/` tree.  They run locally where the
# DB exists (skipif-guarded) and must never ship in the public repo or CI.
tests/test_rp_force_include_h2_acceptance.py
tests/engine_polars/test_benders_stall_e2e.py
tests/calibrate/test_sizing_h2trade_local.py
rivendell/
.aider*
# Local audit / investigation handoffs — meant for personal note-taking,
# not shipped with the repo.  This folder is local-only; never track it.
_audit_reports/
_diagrams/
