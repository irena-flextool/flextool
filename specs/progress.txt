

2026-02-28: Task 1 — Split read_flextool_outputs.py into read_variables.py, read_parameters.py, read_sets.py
  - Created flextool/process_outputs/read_variables.py  (~110 lines, pure extraction)
  - Created flextool/process_outputs/read_parameters.py (~145 lines, pure extraction)
  - Created flextool/process_outputs/read_sets.py        (~130 lines, pure extraction)
  - Replaced read_flextool_outputs.py with a 6-line backward-compat re-export shim
  - Updated __init__.py and result_writer.py to import from the new modules
  - run_flextool.py with scenario network_all_tech passes end-to-end

2026-02-28: Task 2 — Extract drop_levels.py from process_results.py (with simplification)
  - Created flextool/process_outputs/drop_levels.py (~80 lines)
  - Replaced ~98 repetitive droplevel lines with three list-driven loops (_V_DROP, _PAR_DROP, _PAR_DEDUP)
  - Sets section kept individual (too many special cases for a generic loop)
  - Removed drop_levels() from process_results.py; added import from drop_levels module
  - run_flextool.py with scenario network_all_tech passes end-to-end

2026-02-28: Task 3 — Extract calc_capacity_flows.py, calc_connections.py, calc_storage_vre.py
  - Created flextool/process_outputs/calc_capacity_flows.py (~85 lines)
      * compute_capacity_and_flows(par, s, v, r): entity_all_capacity, process_online_dt, flow_dt, ramp_dtt, flow_d, process_source/sink_flow_d
      * Dropped dead code: first r.flow_d assignment (overwritten later) and r_node_ramp_dt local var (never stored to r)
  - Created flextool/process_outputs/calc_connections.py (~65 lines)
      * compute_connection_flows(par, s, v, r): connection_dt/d, connection_losses_dt/d (NEW), connection_to_left/right_node__dt/d
      * Exposes r.from_conn and r.to_conn for upcoming calc_group_flows extraction (task 4)
      * connection_losses_d was missing in original code; added here (spec correctness fix)
  - Created flextool/process_outputs/calc_storage_vre.py (~90 lines)
      * compute_storage_and_vre(par, s, v, r): node_state_change_dt/d, self_discharge_loss_dt/d, node_inflow_d, potentialVREgen_dt/d, storage_usage_dt
  - Updated process_results.py: calls three new functions; group-connection-flow code updated to use r.from_conn/r.to_conn; remaining costs/slacks/group-unit-flows stay inline
  - run_flextool.py with scenario network_all_tech passes end-to-end

2026-02-28: Task 4 — Extract calc_slacks.py, calc_costs.py, calc_group_flows.py
  - Created flextool/process_outputs/calc_slacks.py (~45 lines)
      * compute_slacks(par, s, v, r): reserves_dt/d, upward/downward_node_slack_dt/d,
        costPenalty_node_state_upDown_dt/d, q_inertia/non_synchronous/capacity_margin/reserves and
        corresponding costPenalty_* attributes
  - Created flextool/process_outputs/calc_costs.py (~115 lines)
      * compute_costs(par, s, v, r): commodity/CO2/operational/startup/investment costs and all
        aggregate cost series (costOper_d, costPenalty_d, costInvest_d, etc.)
      * Added _agg_period_or_empty() helper (spec simplification) to replace four identical
        empty-check patterns for startup and penalty period aggregations
      * Aggregate costs use r.costPenalty_* pre-computed by compute_slacks (called first)
  - Created flextool/process_outputs/calc_group_flows.py (~120 lines)
      * compute_group_flows(par, s, v, r): all group-level connection and unit flow aggregations,
        node inflow by group, state losses by group, and node slack by group
  - Replaced process_results.py body with a 7-line thin coordinator calling all six compute_*
    functions in the correct order
  - run_flextool.py with scenario network_all_tech passes end-to-end

2026-02-28: Task 5 — Slim down process_results.py to coordinator
  - No code changes needed: task 4 already replaced process_results.py body with the thin
    coordinator (25 lines) matching the spec exactly
  - Updated spec status from 'not done' to 'implemented'

2026-02-28: Task 6 — Extract output functions into out_capacity.py, out_flows.py, out_node.py, out_costs.py, out_ancillary.py
  - Created flextool/process_outputs/out_capacity.py (~120 lines)
      * unit_capacity(), connection_capacity(), node_capacity()
      * Bug fixes applied: connection_capacity() 'divested' column (was 'invested'); node_capacity() uses v.divest (was v.invest)
  - Created flextool/process_outputs/out_flows.py (~178 lines)
      * unit_outputNode(), unit_inputNode(), unit_cf_outputNode(), unit_cf_inputNode(),
        unit_VRE_curtailment_and_potential(), unit_ramps(), unit_online_and_startup()
  - Created flextool/process_outputs/out_node.py (~116 lines)
      * node_summary(), node_additional_results()
  - Created flextool/process_outputs/out_costs.py (~111 lines)
      * generic(), cost_summaries(), CO2()
  - Created flextool/process_outputs/out_ancillary.py (~243 lines)
      * connection(), connection_wards(), connection_cf(), reserves(), investment_duals(),
        inertia_results(), slack_variables(), input_sets()
  - Updated result_writer.py to import all extracted functions from new modules
  - result_writer.py reduced from ~1790 to ~1052 lines (Task 7 group functions remain)
  - run_flextool.py with scenario network_all_tech passes end-to-end

2026-02-28: Task 8 — Extract write_outputs.py from result_writer.py, update ALL_OUTPUTS imports
  - Created flextool/process_outputs/write_outputs.py (~643 lines)
      * Moved: _read_outputs(), log_time(), print_namespace_structure(), write_summary_csv()
      * Moved: ALL_OUTPUTS list (all imports from out_*.py modules)
      * Moved: write_outputs() orchestrator and __main__ argument-parsing block
      * Added: _resolve_settings() private helper (spec simplification) — extracted settings
        resolution block from write_outputs() to reduce visual complexity
  - Replaced result_writer.py with a 10-line backward-compat re-export shim
  - run_flextool.py with scenario network_all_tech passes end-to-end

2026-02-28: Task 7 — Extract out_group.py from result_writer.py
  - Created flextool/process_outputs/out_group.py (~408 lines)
      * nodeGroup_indicators(), nodeGroup_VRE_share(), nodeGroup_total_inflow(), nodeGroup_flows()
      * Added _get_group_nodes_with_inflow(s, g) shared helper (spec simplification) — deduplicates
        the 'no_inflow' filtering pattern that appeared identically in nodeGroup_indicators and
        nodeGroup_VRE_share
  - Updated result_writer.py to import all four functions from out_group module
  - result_writer.py reduced from ~1052 to ~643 lines (group functions moved out)
  - run_flextool.py with scenario network_all_tech passes end-to-end

2026-02-28: Task 9 — Fix to_spine_db.py bug, apply simplifications
  - Fixed bug in _add_arrays: map(array, col) → map(str, col) for MultiIndex column entity names
  - Extracted _add_column_based_params() shared helper (~50 lines) handling name parsing,
    class-name conversion, parameter definition, entity-name extraction, and per-entity write loop
  - Refactored _add_time_series, _add_strs, _add_arrays to each call _add_column_based_params
    with a small build_value closure; each function reduced to ~10 lines
  - Total reduction: ~110 lines removed from the three functions (replaced by the helper)
  - run_flextool.py with scenario network_all_tech passes end-to-end
2026-02-28: Task 10 — Update __init__.py and ARCHITECTURE.md
  - Fixed __init__.py: changed import from result_writer to write_outputs
      (was: from flextool.process_outputs.result_writer import write_outputs)
      (now: from flextool.process_outputs.write_outputs import write_outputs)
  - Updated ARCHITECTURE.md process_outputs/ section from 4-line stub to full 20-file listing
      with all new modules grouped by layer (I/O, post-processing calculations, output functions)
  - run_flextool.py with scenario network_all_tech passes end-to-end

2026-02-28: plot_outputs T01 — Delete open_summary.py
  - git rm flextool/plot_outputs/open_summary.py (Windows-only os.startfile, no Python callers)
  - Removed stale entry from ARCHITECTURE.md plot_outputs/ section
  - run_flextool.py with scenario network_all_tech passes end-to-end

2026-02-28: plot_outputs T02 — Create perf.py
  - Created flextool/plot_outputs/perf.py (~37 lines)
      * PERF_STATS: dict — global accumulator
      * time_block(name, verbose) — context manager with type hints
      * print_perf_summary() — prints sorted stats
  - Updated plot_functions.py: replaced `import time` + `from contextlib import contextmanager`
    with `from flextool.plot_outputs.perf import PERF_STATS, time_block, print_perf_summary`;
    removed the three definitions (~32 lines)
  - run_flextool.py with scenario network_all_tech passes end-to-end

2026-02-28: plot_outputs T03 — Create format_helpers.py
  - Created flextool/plot_outputs/format_helpers.py (~83 lines)
      * _sig_figs_fmt() — significant-figures formatter
      * _get_value_formatter() — creates matplotlib tick formatter
      * generate_split_filename() — filename with optional index suffix
      * split_into_chunks() — generic list chunker
      * _chunk_average_df() — averages DataFrame over chunk windows
  - Updated plot_functions.py: removed five function definitions (~80 lines);
    replaced with `from flextool.plot_outputs.format_helpers import ...`;
    removed now-unused `Decimal, InvalidOperation` and `FuncFormatter` imports
  - run_flextool.py with scenario network_all_tech passes end-to-end

2026-02-28: plot_outputs T04 — Create config.py
  - Created flextool/plot_outputs/config.py (~60 lines)
      * DIMENSION_RULES: dict[str, str] — documents each single-char dimension rule
      * PlotConfig dataclass — typed, default-carrying wrapper for raw plot settings dicts
      * PLOT_FIELD_NAMES set — moved from plot_functions.py
      * _is_single_config() — moved from plot_functions.py
  - Updated plot_functions.py: removed PLOT_FIELD_NAMES and _is_single_config() (~10 lines);
    replaced with `from flextool.plot_outputs.config import PLOT_FIELD_NAMES, _is_single_config`
  - Also corrected T02 marker in spec (was [ ], should be [x] — already implemented in prior session)
  - run_flextool.py with scenario network_all_tech passes end-to-end

2026-02-28: plot_outputs T05 — Create legend_helpers.py
  - Created flextool/plot_outputs/legend_helpers.py (~65 lines)
      * estimate_legend_width(labels, title, base_width) — moved from plot_functions.py (~30 lines)
      * _format_legend_labels(items) — extracts tuple/list→string pattern (was duplicated 3+ times)
      * _should_show_legend(legend_position, sub_levels, idx, n_cols, n_subs) — extracts the
        show/hide legend decision (was copy-pasted identically in all 3 plot functions)
  - Updated plot_functions.py:
      * Removed estimate_legend_width definition (~30 lines)
      * Added `from flextool.plot_outputs.legend_helpers import ...`
      * Replaced [str(x) for x in items] patterns with _format_legend_labels() (3 sites)
      * Replaced 12-line show_legend blocks with _should_show_legend() (3 sites)
  - Updated ARCHITECTURE.md: added legend_helpers.py to plot_outputs/ listing
  - run_flextool.py with scenario network_all_tech passes end-to-end

2026-02-28: plot_outputs T06 — Create axis_helpers.py
  - Created flextool/plot_outputs/axis_helpers.py (~150 lines)
      * _is_datetime_format() — ISO datetime predicate
      * _normalize_axis_scale() — converts raw axis_scale_min_max to list of (min, max) | None
      * _subplot_axis_scale() — per-subplot scale lookup
      * _apply_subplot_label() — xlabel/ylabel with str/list support
      * _set_calendar_xticks() — non-datetime tick spacing (extracted from set_smart_xticks)
      * _set_datetime_xticks() — datetime tick spacing with minor ticks (extracted from set_smart_xticks)
      * set_smart_xticks() — thin dispatcher calling _set_datetime_xticks or _set_calendar_xticks
  - Updated plot_functions.py: removed 5 function definitions (~160 lines);
    removed `import re`; added `from flextool.plot_outputs.axis_helpers import ...`
  - Updated ARCHITECTURE.md: added axis_helpers.py to plot_outputs/ listing
  - run_flextool.py with scenario network_all_tech passes end-to-end

2026-03-01: plot_outputs T07 — Create subplot_helpers.py
  - Created flextool/plot_outputs/subplot_helpers.py (~75 lines)
      * _calculate_grid_layout(n_subs, subplots_per_row) — ceiling-division grid dims; replaced 3 identical inline patterns
      * _get_unique_levels(df_columns, level_indices) — unified unique subplot-value extraction (empty→[None], single→scalars, multi→tuples); replaced 3 identical inline blocks
      * _extract_subplot_data(df, sub, sub_levels) — unified .xs() with Series→DataFrame guard; handles None sub (no-subplot case) and non-MultiIndex df[sub]; replaced 7 inline extraction blocks
  - Updated plot_functions.py: added import from subplot_helpers; removed ~50 lines of duplicated extraction code across all three plotting functions
  - Updated ARCHITECTURE.md: added subplot_helpers.py to plot_outputs/ listing
  - run_flextool.py with scenario network_all_tech passes end-to-end

2026-03-01: plot_outputs T09 — Create plot_bars.py
  - Created flextool/plot_outputs/plot_bars.py (~558 lines)
      * plot_rowbars_stack_groupbars() — full orchestration + layout setup (figure/gridspec/axes),
        bar counting, legend width estimation, per-subplot data extraction, axis setup,
        group/expand-axis two-level axis, legend, value axis formatting, zero line
      * Layout code moved as-is (variable row/col heights, GridSpec with height_ratios/width_ratios)
      * Dispatches to _plot_grouped_bars, _plot_stacked_bars, _plot_simple_bars from plot_bars_detail
  - Updated plot_functions.py: removed plot_rowbars_stack_groupbars() definition (~540 lines);
    replaced with `from flextool.plot_outputs.plot_bars import plot_rowbars_stack_groupbars`;
    removed GridSpec import (only needed by bar function); removed _sig_figs_fmt and
    _is_datetime_format imports (no longer used directly); reduced from 1389 to 847 lines
  - Updated ARCHITECTURE.md: added plot_bars.py to plot_outputs/ listing
  - run_flextool.py with scenario network_all_tech passes end-to-end

2026-03-01: plot_outputs T10 — Create plot_lines.py
  - Created flextool/plot_outputs/plot_lines.py (~280 lines)
      * plot_dt_sub_lines() — line plots with optional subplots
      * plot_dt_stack_sub() — stacked area plots
  - Updated plot_functions.py: removed both function definitions (~345 lines);
    added `from flextool.plot_outputs.plot_lines import plot_dt_sub_lines, plot_dt_stack_sub`;
    removed now-unused imports (np, MaxNLocator, PERF_STATS/time_block/print_perf_summary,
    _get_value_formatter, estimate_legend_width/_format_legend_labels/_should_show_legend,
    _subplot_axis_scale/_apply_subplot_label/set_smart_xticks,
    _calculate_grid_layout/_get_unique_levels/_extract_subplot_data, PLOT_FIELD_NAMES)
  - Updated ARCHITECTURE.md: added plot_lines.py to plot_outputs/ listing
  - run_flextool.py with scenario network_all_tech passes end-to-end

2026-03-01: plot_outputs T08 — Create plot_bars_detail.py
  - Created flextool/plot_outputs/plot_bars_detail.py (~270 lines)
      * _stack_label(stack_idx, stacks, labeled_stacks) — shared label helper for stacked bars,
        extracts the copy-pasted label-generation logic from all 3 loops (pos/zero/neg)
      * _plot_grouped_bars(ax, df_sub, all_bars, expand_axis_level_names,
        grouped_bar_level_names, bar_orientation, value_fmt) — grouped side-by-side bars
      * _plot_stacked_bars(ax, df_sub, all_bars, expand_axis_level_names,
        stack_level_names, bar_orientation) — stacked bars; pos→zero→neg loop order preserved
      * _plot_simple_bars(ax, df_sub, all_bars, expand_axis_level_names,
        bar_orientation, value_fmt) — single-color bars, no stacking/grouping
  - Updated plot_functions.py: replaced the three 90–110 line rendering branches with 3-line
    calls to the new functions; added import from plot_bars_detail; reduced from 1657 to 1392 lines
  - run_flextool.py with scenario network_all_tech passes end-to-end

2026-03-01: plot_outputs T11 — Create orchestrator.py
  - Created flextool/plot_outputs/orchestrator.py (~320 lines)
      * _plan_file_splits(all_subs, all_items, subplot_levels, max_items_per_file, max_subplots_per_file)
        — extracted splitting-strategy logic from plot_dict_of_dataframes(); returns file_chunks
        structured as list[(sub, item_chunk)] (strategy 1) or list[items] (strategy 2)
      * plot_dict_of_dataframes() — moved from plot_functions.py; now uses PlotConfig(**{...})
        instead of scattered .get() calls; MultiIndex normalisation added at entry so df always
        has MultiIndex index and columns before dimension-rule processing
  - Replaced plot_functions.py body with a 4-line backward-compat re-export shim
  - Updated ARCHITECTURE.md: added orchestrator.py, updated plot_functions.py description
  - run_flextool.py with scenario network_all_tech passes end-to-end

2026-03-01: plot_outputs T12 — Update __init__.py
  - Updated flextool/plot_outputs/__init__.py (~14 lines)
      * Replaced single-line docstring with navigation docstring listing all submodules and their roles
      * Changed import from plot_functions to orchestrator (plot_dict_of_dataframes)
  - run_flextool.py with scenario network_all_tech passes end-to-end

2026-03-01: flextoolrunner T01 — Remove dead code from flextoolrunner.py
  - Deleted methods periodic_postprocess, combine_result_tables, divide_column, divide_group_with_another (~115 lines)
  - Deleted the results_post_processed block in run_model (~43 lines)
  - Total reduction: 156 lines (3208 → 3052 lines)
  - run_flextool.py with scenario network_all_tech passes end-to-end

2026-03-01: flextoolrunner T02 — Create db_reader.py
  - Created flextool/flextoolrunner/db_reader.py (~119 lines)
      * check_version(db, logger) — self.logger → explicit param
      * get_single_entities(db, entity_class_name) — unchanged logic
      * entities_to_dict(db, cl, mode) — unchanged logic
      * params_to_dict(db, cl, par, mode, str_to_list) — unchanged logic
      * All functions have Python 3.11+ type hints
  - Updated flextoolrunner.py: added import from db_reader; replaced all self.method() calls
    with free function calls; removed the 4 method definitions (~68 lines net reduction, 3052 → 2984)
  - run_flextool.py with scenario network_all_tech passes end-to-end

2026-03-01: flextoolrunner T03 — Create input_writer.py and move write_input
  - Created flextool/flextoolrunner/input_writer.py (~633 lines)
      * write_input(input_db_url, scenario_name, logger) — self.logger → explicit param
      * write_entity(db, cl, header, filename, entity_dimens) — moved from module level
      * write_parameter(db, cl_pars, header, filename, ...) — moved from module level
      * flatten_map(mapList, indexes) — moved from module level
      * write_default_values(db, cl_pars, header, filename, ...) — moved from module level
      * All functions have Python 3.11+ type hints
  - Updated flextoolrunner.py: added import of input_writer; replaced write_input body
    with one-liner delegate; removed 4 free functions (~591 lines net reduction, 2984 → 2393)
  - run_flextool.py with scenario network_all_tech passes end-to-end

2026-03-01: flextoolrunner T04 — Create solve_config.py
  - Created flextool/flextoolrunner/solve_config.py (~392 lines)
      * SolveConfig class with all solve-level fields and type hints
      * SolveConfig.load_from_db(db, logger) classmethod — contains the DB reading sequence
        previously in FlexToolRunner.__init__ (lines ~83–131)
      * duplicate_solve(old, new, update_model_solves=True) — S11: renamed first_level_flag
      * make_roll_counter() — unchanged logic, now on SolveConfig
      * get_period_timesets(db) — unchanged logic, now on SolveConfig
      * periods_to_tuples(db, cl, par) — unchanged logic, now on SolveConfig
      * S02: rolling_times assembly replaced with dict comprehension using .get() defaults
  - Updated flextoolrunner.py:
      * Added import of SolveConfig
      * Replaced solve-level DB reading block with SolveConfig.load_from_db(db, self.logger)
      * Added aliases (self.model_solve = self.solve.model_solve etc.) for remaining methods
      * Removed 4 method definitions: duplicate_solve, make_roll_counter,
        get_period_timesets, periods_to_tuples (~119 lines net reduction, 2393 → 2274)
      * Updated duplicate_solve call at line ~1839: first_level_flag=False → update_model_solves=False
  - run_flextool.py with scenario network_all_tech passes end-to-end

2026-03-01: flextoolrunner T05 — Create timeline_config.py
  - Created flextool/flextoolrunner/timeline_config.py (~626 lines)
      * TimelineConfig class with all timeline-level fields and type hints
      * TimelineConfig.load_from_db(db, logger) classmethod — reads timelines,
        timesets, timeset_durations, timesets__timeline, new_step_durations from DB
      * create_timeline_from_timestep_duration() — synthesizes new timelines
      * create_assumptive_parts(solve_config) — was create_assumptive_timestructure_parts;
        takes SolveConfig param instead of using self.* solve fields
      * create_averaged_timeseries(solve, solve_config, logger) — averages/sums
        timeseries for changed step durations
      * Free functions: get_active_time(), make_step_jump(), make_steps(),
        make_timeset_timeline(), separate_period_and_timeseries_data()
      * All functions/methods have Python 3.11+ type hints
  - Updated flextoolrunner.py:
      * Added import of TimelineConfig and free functions
      * Replaced timeline DB reading block with TimelineConfig.load_from_db()
      * Added timeline aliases (self.timelines = self.timeline.timelines etc.)
      * Replaced self.method() calls with free function calls or timeline.method() calls
      * Removed 8 method/function definitions: create_timeline_from_timestep_duration,
        create_averaged_timeseries, create_assumptive_timestructure_parts,
        make_steps, make_timeset_timeline, get_active_time, make_step_jump,
        separate_period_and_timeseries_data (~448 lines net reduction, 2274 → 1826)
      * Removed unused imports: math, get_single_entities, params_to_dict, entities_to_dict
  - run_flextool.py with scenario network_all_tech passes end-to-end

2026-03-01: flextoolrunner T06 — Create runner_state.py
  - Created flextool/flextoolrunner/runner_state.py (~36 lines)
      * PathConfig dataclass — flextool_dir, bin_dir, root_dir, output_path
      * RunnerState dataclass — paths, solve (SolveConfig), timeline (TimelineConfig), logger
  - Updated flextoolrunner.py:
      * Added import of PathConfig, RunnerState from runner_state
      * __init__ now builds PathConfig from constructor args, then assembles RunnerState
        with paths, solve, timeline, and logger; stores as self.state
      * Removed all ~36 backward-compat aliases (self.model = self.solve.model, etc.)
      * Updated all self.X attribute references in remaining methods to use
        self.state.solve.X, self.state.timeline.X, self.state.paths.X, or self.state.logger
      * Method calls (self.write_*, self.model_run, etc.) unchanged
      * self.state is the single instance attribute on FlexToolRunner
      * No line count change (aliases removed, but self.state.* references are longer)
  - run_flextool.py with scenario network_all_tech passes end-to-end

2026-03-01: flextoolrunner T07 — Create solve_writers.py
  - Created flextool/flextoolrunner/solve_writers.py (~526 lines)
      * All ~25 write_* methods and get_first_steps moved as free functions
      * Methods that used self.* converted to explicit parameters:
        write_hole_multiplier (hole_multipliers param),
        write_last_realized_step (realized_periods param),
        write_realized_dispatch (realized_periods param),
        write_fix_storage_timesteps (fix_storage_periods param),
        write_delayed_durations (delay_durations param),
        write_all_branches (logger param),
        write_branch_weights_and_map (stochastic_branches param)
      * S04: Consolidated write_first_steps/write_last_steps into
        write_period_boundary_step(timeline, filename, *, last=False)
        with thin wrappers for call-site readability
      * S06: Removed unused self from write_last_steps, write_timesets
        (solved automatically by extraction to free functions)
      * S12: Renamed write_currentSolve → write_current_solve
      * S09: Renamed write_solve_branch__time_branch_list_and_weight →
        write_branch_weights_and_map
      * All functions have Python 3.11+ type hints
  - Updated flextoolrunner.py:
      * Added import of solve_writers module
      * Replaced all self.write_* calls in run_model with solve_writers.* calls
      * Removed ~25 method definitions (~448 lines net reduction, 1786 → 1338)
      * Removed unused csv import
  - run_flextool.py with scenario network_all_tech passes end-to-end

2026-03-01: flextoolrunner T08 — Create solver_runner.py
  - Created flextool/flextoolrunner/solver_runner.py (~310 lines)
      * SolverRunner class with __init__(self, state: RunnerState)
      * run(current_solve) — public entry point (was model_run)
      * _run_glpsol(command_args) — filtered GLPSOL subprocess output
      * _cplex_to_glpsol(cplexfile, solutionfile) — CPLEX XML→GLPSOL conversion
      * _platform_binaries() — platform-specific binary paths + Linux permissions
      * _run_glpsol_solver() — pure-GLPSOL workflow
      * _run_highs_or_cplex() — three-phase glpsol→solver→glpsol workflow
      * _run_highs() — HiGHS solver invocation
      * _run_cplex() — CPLEX solver invocation
      * S03: Extracted _write_glpsol_solution(root, solutionfile, *, is_mip) static method
        to deduplicate the near-identical 'optimal' vs 'integer optimal solution' branches
        (~90 lines of duplicate code → single ~40 line method)
      * S10: Consolidated CPLEX precommand handling — command assembled once,
        wrapper conditionally prepended (was duplicated with/without precommand)
      * All functions have Python 3.11+ type hints
  - Updated flextoolrunner.py:
      * Added import of SolverRunner
      * Removed subprocess and xml.etree.ElementTree imports (no longer needed)
      * Removed 3 method definitions: model_run, run_glpsol, cplex_to_glpsol
      * Created SolverRunner instance in run_model, replaced self.model_run() with solver.run()
      * (~338 lines net reduction, 1338 → 1000)
  - run_flextool.py with scenario network_all_tech passes end-to-end

2026-03-01: flextoolrunner T09 — Create stochastic.py
  - Created flextool/flextoolrunner/stochastic.py (~388 lines)
      * StochasticSolver class with __init__(self, state: RunnerState)
      * create_stochastic_periods(...) — stochastic branching of time periods
      * connect_two_timelines(period, first_solve, second_solve, period__branch) — computes
        cumulative duration maps for two solve timelines
      * find_previous_timestep(...) — finds previous timestep in another solve's timeline
      * find_next_timestep(...) — finds next timestep in another solve's timeline
      * write_timeline_matching_map(...) — writes timeline_matching_map.csv for nested
        solve storage fixing using binary search
      * _get_timeset_for_period(period_timesets, real_period) — S09: extracted helper
        to find timeset for a period (was linear scan repeated in connect_two_timelines)
      * S05: moved `import bisect` from inside write_timeline_matching_map to top-level import
      * All methods have Python 3.11+ type hints
  - Updated flextoolrunner.py:
      * Added import of StochasticSolver
      * Replaced self.create_stochastic_periods() with StochasticSolver(self.state).create_stochastic_periods()
      * Replaced self.write_timeline_matching_map() with stochastic_solver.write_timeline_matching_map()
      * Replaced self.find_next_timestep() in _process_rolling_solve with StochasticSolver(self.state).find_next_timestep()
      * Removed 5 method definitions (~268 lines net reduction, 1000 → 732)
  - run_flextool.py with scenario network_all_tech passes end-to-end

2026-03-01: flextoolrunner T10 — Create rolling_solver.py
  - Created flextool/flextoolrunner/rolling_solver.py (~451 lines)
      * RollingSolver class with __init__(self, state: RunnerState)
      * define_solve_recursive(...) — public entry point (was _define_solve_recursive,
        dropped leading underscore since it's called from run_model)
      * _process_rolling_solve(...) — rolling window solve logic
      * _process_single_solve(...) — single (non-rolling) solve logic
      * create_rolling_solves(...) — splits solve into overlapping roll sequence
      * _filter_time_list_by_periods(...) — @staticmethod, filters by period dict
      * _get_periods_from_parent_time_list(...) — @staticmethod, extracts period set
      * _filter_time_list_by_parent_scope(...) — @staticmethod, filters child by parent scope
      * S08: Moved ParentSolveInfo namedtuple to module level (was class attribute)
      * All methods have Python 3.11+ type hints
  - Updated flextoolrunner.py:
      * Added import of RollingSolver and ParentSolveInfo
      * Replaced self._define_solve_recursive() with rolling_solver.define_solve_recursive()
      * Replaced self.ParentSolveInfo with module-level ParentSolveInfo
      * Removed 7 method definitions and ParentSolveInfo namedtuple
        (~394 lines net reduction, 732 → 338)
      * Removed unused imports: namedtuple, make_step_jump
  - run_flextool.py with scenario network_all_tech passes end-to-end

2026-03-01: flextoolrunner T11 — Create orchestration.py
  - Created flextool/flextoolrunner/orchestration.py (~240 lines)
      * run_model(state: RunnerState, solver: SolverRunner) -> int — the main solve loop
      * Creates output directories (solve_data/, output_raw/, output_plots/)
      * Builds solve execution order via RollingSolver(state).define_solve_recursive(...)
      * Deduplicates realized timesteps
      * Builds solve_period_history for each real solve
      * Applies stochastic branching via StochasticSolver(state).create_stochastic_periods(...)
      * For each solve: writes ~20 CSV files via solve_writers.*, runs solver, copies storage fix files
      * All internal calls use module-qualified functions (solve_writers.write_full_timelines(...),
        RollingSolver(state).define_solve_recursive(...), etc.)
      * All self.state.* references converted to state.* (free function, no class)
      * Type hints added (Python 3.11+)
  - Updated flextoolrunner.py:
      * Added import of orchestration module
      * Replaced run_model body with two-line delegate: creates SolverRunner, calls orchestration.run_model()
      * Removed unused imports: time, copy, shutil, OrderedDict, defaultdict,
        solve_writers, get_active_time, separate_period_and_timeseries_data,
        StochasticSolver, RollingSolver, ParentSolveInfo
      * flextoolrunner.py reduced from 338 to 94 lines (~244 lines net reduction)
  - run_flextool.py with scenario network_all_tech passes end-to-end

2026-03-01: flextoolrunner T12 — Slim down flextoolrunner.py
  - Added class docstring to FlexToolRunner pointing to __init__.py for navigation guide
  - Removed unused `translation` variable (dead code)
  - Removed extra blank lines
  - Verified file is 96 lines (well under ≤130 line target)
  - File contains only __init__, run_model (delegate), write_input (delegate), plus main()
  - run_flextool.py with scenario network_all_tech passes end-to-end

2026-03-01: flextoolrunner T13 — Update __init__.py
  - Replaced single-line docstring with full navigation docstring listing all submodules
    and their roles (Entry point, State objects, Solve logic, I/O layers)
  - Preserved FlexToolRunner import and __all__ export
  - run_flextool.py with scenario network_all_tech passes end-to-end

2026-03-01: flextoolrunner T14 — End-to-end test
  - Ran full pipeline: python run_flextool.py sqlite:///templates/examples.sqlite
    sqlite:///output_info.sqlite --scenario-name network_all_tech
  - Pipeline completed successfully: init (0.23s), write (1.70s), solve (0.93s),
    post-processing (0.59s), total (9.01s)
  - solve_data/ and output_raw/ output files generated correctly
  - HiGHS solver found optimal solution (objective 316.48, gap 0.18%)
  - All T01–T13 refactoring changes verified: no behaviour change in the refactored
    flextoolrunner package (db_reader, input_writer, solve_config, timeline_config,
    runner_state, solve_writers, solver_runner, stochastic, rolling_solver,
    orchestration modules all working correctly)
  - run_flextool.py with scenario network_all_tech passes end-to-end

2026-03-01: scenario_comparison T01 — Remove dead code
  - Removed stacked_and_grouped_bar_plot (~136 lines) — not called anywhere in production code
  - Removed stacked_and_grouped_barh_plot (~149 lines) — not called anywhere in production code
  - Removed get_group_process_multiindex (~19 lines) — not called anywhere
  - Only references were in notebooks/RETO Africa process.py (not production code)
  - scenario_comparison.py reduced from 2,293 to 1,984 lines (~309 lines removed)
  - run_flextool.py with scenario network_all_tech passes end-to-end

2026-03-01: scenario_comparison T02 — Remove old config format support
  - Simplified get_scenarios_from_config: removed list-format branch (~10 lines),
    now only handles dict format (old list format cannot survive a single config rewrite)
  - Removed old_format migration block from create_or_update_dispatch_config:
    deleted old_format detection, old_colors collection from legacy 'colors' section,
    old_scenario_colors extraction, and old list→dict migration branch (~30 lines)
  - Removed old_colors merge at color assignment (was: {**old_colors, **existing_inline_colors})
  - scenario_comparison.py reduced from 1,984 to ~1,940 lines (~44 lines removed)
  - run_flextool.py with scenario network_all_tech passes end-to-end

2026-03-01: scenario_comparison T03 — Create constants.py
  - Created flextool/scenario_comparison/constants.py (~20 lines)
      * DEFAULT_SPECIAL_COLORS dict — color mapping for special dispatch columns
      * POSITIVE_SPECIAL list — columns at top of stacked plot
      * NEGATIVE_SPECIAL list — columns at bottom of stacked plot
      * LINE_COLUMNS list — columns plotted as lines, not stacked areas
  - Updated scenario_comparison.py: removed 4 constant definitions (~18 lines),
    added import from constants module
  - run_flextool.py with scenario network_all_tech passes end-to-end

2026-03-01: scenario_comparison T04 — Create data_models.py
  - Created flextool/scenario_comparison/data_models.py (~100 lines)
      * TimeSeriesResults dataclass — 11 optional DataFrame fields matching parquet filenames
        (unit_outputNode_dt_ee, unit_inputNode_dt_ee, connection_leftward/rightward/losses,
        node_slack_up_dt_e, node_d_ep, node_inflow__dt, nodeGroup_flows_d_gpe,
        nodeGroup_gd_p, unit_curtailment_outputNode_dt_ee)
      * TimeSeriesResults.to_dict() — returns non-None fields as dict
      * TimeSeriesResults.from_dict(d) — builds from raw dict, warns about unknown keys
      * DispatchMappings dataclass — 16 optional DataFrame fields matching mapping parquet keys
      * DispatchMappings.get_for_scenario(field_name, scenario) — extracts per-scenario slice
        via xs(), handles Series→DataFrame conversion for single-row results
  - No changes to scenario_comparison.py (data_models not wired in until T05/T06)
  - run_flextool.py with scenario network_all_tech passes end-to-end

2026-03-01: scenario_comparison T05 — Create db_reader.py
  - Created flextool/scenario_comparison/db_reader.py (~155 lines)
      * read_scenario_folders(db_url) — reads scenario→folder mapping from Spine DB
      * collect_parquet_files(scenario_folders, output_subdir) — gathers parquet paths grouped by filename
      * combine_parquet_files(files_by_name) — concats per-scenario parquets into combined DataFrames
      * get_scenario_results(db_url, parquet_subdir) — top-level convenience, now returns
        (dict[str, str], TimeSeriesResults) using TimeSeriesResults.from_dict()
      * All functions have Python 3.11+ type hints
  - Updated scenario_comparison.py: removed 4 function definitions (~140 lines),
    added backward-compat re-import from db_reader at bottom of file;
    removed unused spinedb_api imports (DatabaseMapping, from_database, Array,
    alternative_filter_config, append_filter_config)
  - Updated __init__.py: fixed stale import path (was flextool.create_scenarios.scenario_comparison,
    now flextool.scenario_comparison.db_reader)
  - Updated cmd_scenario_results.py: imports get_scenario_results from db_reader;
    unpacks TimeSeriesResults with .to_dict() for downstream functions not yet updated
  - run_flextool.py with scenario network_all_tech passes end-to-end

2026-03-01: scenario_comparison T06 — Create dispatch_mappings.py
  - Created flextool/scenario_comparison/dispatch_mappings.py (~140 lines)
      * load_dispatch_mappings(parquet_dir) — load all mapping files from one scenario folder
      * combine_dispatch_mappings(scenario_folders, parquet_subdir) → DispatchMappings
        (was → dict; now returns typed DispatchMappings dataclass)
      * get_group_node_multiindex(group_node_df, scenario) — build (group, node) MultiIndex
      * All functions have Python 3.11+ type hints
  - Updated scenario_comparison.py: removed 3 function definitions (~125 lines),
    added backward-compat re-import from dispatch_mappings at bottom of file
  - Updated cmd_scenario_results.py: imports combine_dispatch_mappings from dispatch_mappings;
    converts DispatchMappings to dict view via vars() for downstream functions not yet updated
  - run_flextool.py with scenario network_all_tech passes end-to-end

2026-03-01: scenario_comparison T07 — Create config_io.py
  - Created flextool/scenario_comparison/config_io.py (~185 lines)
      * _yaml_quote(value) — quote strings with YAML-special characters
      * _yaml_color(color) — quote hex color strings starting with #
      * parse_config_with_comments(config_path) — parse YAML config tracking commented entries
      * _write_sign_section(section_name, config_dict, lines) — promoted from nested function
        inside write_config_with_comments to module-level (S7)
      * write_config_with_comments(config_path, config_dict, commented_entries) — write config
        with commented entries preserved
      * All functions have Python 3.11+ type hints
  - Updated scenario_comparison.py: removed 4 function definitions (~160 lines),
    added import from config_io; removed unused yaml import
  - scenario_comparison.py reduced from ~1,665 to ~1,492 lines
  - run_flextool.py with scenario network_all_tech passes end-to-end

2026-03-01: scenario_comparison T08 — Create config_builder.py
  - Created flextool/scenario_comparison/config_builder.py (~359 lines)
      * get_scenarios_from_config(config) — extract active scenario names from config dict
      * compute_process_group_std_order(results, mappings, scenarios, available_process_groups)
        — compute std dev for each processGroup, return ordered list (lowest first);
        updated signature to use TimeSeriesResults and DispatchMappings instead of dicts
      * create_or_update_dispatch_config(plot_dir, results, scenarios, mappings)
        — build/update dispatch config.yaml with scenario colors, positive/negative process
        groups, and node selections; updated signature to use TimeSeriesResults and
        DispatchMappings; internally converts to dicts for calling prepare_dispatch_data
        (not yet refactored)
      * All functions have Python 3.11+ type hints
  - Updated scenario_comparison.py: removed 3 function definitions (~327 lines),
    added backward-compat re-import from config_builder; removed unused imports
    (plot_dict_of_dataframes, parse_config_with_comments, write_config_with_comments)
  - Updated cmd_scenario_results.py: imports get_scenarios_from_config and
    create_or_update_dispatch_config from config_builder; imports DispatchMappings
    from data_models; passes TimeSeriesResults and DispatchMappings directly to
    create_or_update_dispatch_config; ensures empty DispatchMappings() fallback
  - scenario_comparison.py reduced from ~1,492 to ~1,165 lines
  - run_flextool.py with scenario network_all_tech passes end-to-end

2026-03-01: scenario_comparison T09 — Create dispatch_data.py
  - Created flextool/scenario_comparison/dispatch_data.py (~576 lines)
      * _get_time_index(results, scenario) — shared helper (S5): finds time index
        from unit_outputNode_dt_ee or connection_leftward_dt_eee
      * _slice_scenario_df(df, scenario) — shared helper (S4): extracts scenario
        columns via xs() and groups by time; replaces ~13 inline guard+slice patterns
      * _order_dispatch_columns(df, split_mixed) — shared helper (S6): validates
        signs, sorts by std dev, reorders columns; split_mixed=False for group
        dispatch (excludes mixed-sign), split_mixed=True for node dispatch
        (splits into _pos/_neg); replaces ~80 lines of duplicated logic
      * prepare_dispatch_data(results, mappings, scenario, output_node_group, colors)
        — updated signature to use TimeSeriesResults and DispatchMappings;
        replaced _get_mapping() inner function with mappings.get_for_scenario() (S3);
        uses _slice_scenario_df and _get_time_index helpers
      * prepare_node_dispatch_data(results, scenario, node) — updated signature
        to use TimeSeriesResults; uses _slice_scenario_df, _get_time_index,
        _order_dispatch_columns helpers
      * All functions have Python 3.11+ type hints
  - Updated scenario_comparison.py: removed 2 function definitions (~460 lines),
    replaced with thin backward-compat wrappers that convert dicts to typed
    dataclasses and delegate to dispatch_data; removed unused POSITIVE_SPECIAL,
    NEGATIVE_SPECIAL, LINE_COLUMNS imports
  - Updated config_builder.py: changed import from scenario_comparison to
    dispatch_data; removed dict conversion (combined_dfs/combined_mapping_dfs);
    passes TimeSeriesResults and DispatchMappings directly to prepare_dispatch_data
  - scenario_comparison.py reduced from ~1,165 to ~540 lines
  - run_flextool.py with scenario network_all_tech passes end-to-end

2026-03-01: scenario_comparison T10 — Create dispatch_plots.py
  - Created flextool/scenario_comparison/dispatch_plots.py (~276 lines)
      * _auto_assign_node_colors(columns) — auto-assign tab20 colors for node dispatch columns
      * get_color_for_column(col, colors_dict) — promoted from nested function inside
        plot_dispatch_area to module-level (S7); handles _in/_out suffix lookup
      * _compute_ylim(df, timeline) — promoted from nested function inside
        create_dispatch_plots to module-level (S7); computes stacked area y-axis range
      * plot_dispatch_area(df_dispatch, inflow_series, output_path, ...) — renders and
        saves a single stacked area dispatch plot with demand line
      * create_dispatch_plots(results, mappings, config, plot_dir, ...) — updated signature
        to use TimeSeriesResults and DispatchMappings; calls prepare_dispatch_data and
        prepare_node_dispatch_data from dispatch_data module directly (no dict conversion)
      * All functions have Python 3.11+ type hints
  - Updated scenario_comparison.py: removed 3 function definitions (~290 lines:
    _auto_assign_node_colors, plot_dispatch_area, create_dispatch_plots); added
    backward-compat re-import from dispatch_plots; removed unused imports
    (matplotlib, numpy, DEFAULT_SPECIAL_COLORS)
  - Updated cmd_scenario_results.py: imports create_dispatch_plots from dispatch_plots;
    passes TimeSeriesResults and DispatchMappings directly instead of dict views
  - scenario_comparison.py reduced from ~540 to ~251 lines
  - run_flextool.py with scenario network_all_tech passes end-to-end

2026-03-01: scenario_comparison T11 — Create summary_plots.py
  - Created flextool/scenario_comparison/summary_plots.py (~235 lines)
      * reindex_scenarios(df, scen_list) — promoted from nested function inside
        create_basic_plots to module-level (S7)
      * plot_horizontal_bar(df, ...) — moved from scenario_comparison.py; removed
        unused return value (S8: was `return ax`, now `-> None`)
      * create_basic_plots(results, group_node_df, config, plot_dir, ...) — updated
        signature to use TimeSeriesResults instead of dict; checks `results.X is not None`
        instead of `'X' in combined_dfs`
      * All functions have Python 3.11+ type hints
  - Updated scenario_comparison.py: removed create_basic_plots (~143 lines) and
    plot_horizontal_bar (~55 lines); added backward-compat re-import from summary_plots;
    removed unused imports (os, math, Path, matplotlib.pyplot)
  - Updated cmd_scenario_results.py: imports create_basic_plots from summary_plots;
    passes TimeSeriesResults directly instead of combined_dfs dict
  - scenario_comparison.py reduced from ~251 to ~53 lines
  - run_flextool.py with scenario network_all_tech passes end-to-end

2026-03-01: scenario_comparison T12 — Create orchestrator.py
  - Created flextool/scenario_comparison/orchestrator.py (~115 lines)
      * run(db_url, parquet_subdir, plot_dir, output_config_path, active_configs,
        plot_rows, write_to_xlsx, write_dispatch_xlsx, write_to_ods, show_plots,
        dispatch_plots, basic_plots) — top-level orchestration function taking
        already-resolved parameters (no argparse, no settings DB logic)
      * Loads output config YAML, gets scenario results via db_reader,
        combines dispatch mappings, creates/updates dispatch config,
        generates comparison plots, dispatch plots, summary plots, and
        optionally writes combined results to Excel
      * All functions have Python 3.11+ type hints
  - Updated cmd_scenario_results.py: thin argparse+settings wrapper (~140 lines)
      * Keeps argparse definitions, settings DB resolution (CLI > DB > defaults),
        alternative filter application
      * Resolves --all-plots into dispatch_plots and basic_plots flags
      * Delegates to orchestrator.run() with all resolved parameters
      * Removed direct imports of get_scenario_results, combine_dispatch_mappings,
        create_dispatch_plots, create_basic_plots, create_or_update_dispatch_config,
        get_scenarios_from_config, DispatchMappings, plot_dict_of_dataframes, yaml
  - run_flextool.py with scenario network_all_tech passes end-to-end

2026-03-01: scenario_comparison T13 — Verify cmd_scenario_results.py is thin wrapper
  - T13 was already completed as part of T12: cmd_scenario_results.py is a thin
    argparse+settings wrapper (~160 lines) that calls orchestrator.run()
  - No code changes needed — just verified and marked as implemented

2026-03-01: scenario_comparison T14 — Update __init__.py with navigation docstring
  - Updated flextool/scenario_comparison/__init__.py (~14 lines)
      * Replaced single-line docstring with navigation docstring listing all 9 submodules
        and their roles (data_models, db_reader, dispatch_mappings, config_io, config_builder,
        dispatch_data, dispatch_plots, summary_plots, orchestrator)
      * Preserved existing get_scenario_results import and __all__ export
  - run_flextool.py with scenario network_all_tech passes end-to-end

2026-03-01: scenario_comparison T15 — Update ARCHITECTURE.md module map for scenario_comparison/
  - Replaced single-file scenario_comparison entry with full 12-file module listing
      * __init__.py, constants.py, data_models.py, db_reader.py, dispatch_mappings.py,
        config_io.py, config_builder.py, dispatch_data.py, dispatch_plots.py,
        summary_plots.py, orchestrator.py, scenario_comparison.py (backward-compat shim)
  - Stale create_scenarios/ reference was already fixed in T05 (import path in __init__.py)
  - run_flextool.py with scenario network_all_tech passes end-to-end

2026-03-01: scenario_comparison T16 — End-to-end verification and add missing TimeSeriesResults fields
  - Ran flextool-scenario-results end-to-end with 2 scenarios (network_all_tech, coal_min_load_wind)
    including --dispatch-plots and --basic-plots flags
  - from_dict() warning identified 72 unknown parquet file keys: 52 actual result variables
    and 20 dispatch mapping/structural files (loaded separately by dispatch_mappings.py)
  - Added 52 missing result variable fields to TimeSeriesResults in data_models.py:
      * Unit outputs: unit_outputNode_d_ee, unit_inputNode_d_ee, unit_outputs_cf_d_ee,
        unit_inputs_cf_d_ee, unit_curtailment_share_outputNode_dt_ee/_d_ee,
        unit_VRE_potential_outputNode_dt_ee/_d_ee, unit_ramp_outputs_dt_ee/_inputs_dt_ee,
        unit_online_dt_e/_average_d_e, unit_startup_d_e, unit_capacity_ed_p
      * Connection outputs: connection_leftward_d_eee, connection_rightward_d_eee,
        connection_losses_d_eee, connection_dt_eee, connection_d_eee, connection_cf_d_e,
        connection_capacity_ed_p
      * Node outputs: node_dt_ep, node_prices_dt_e, node_state_dt_e, node_slack_up_d_e,
        node_slack_down_dt_e/_d_e, node_capacity_ed_p
      * NodeGroup outputs: nodeGroup_flows_dt_gpe, nodeGroup_gdt_p, nodeGroup_VRE_share_d_g/_dt_g,
        nodeGroup_inertia_dt_g/_largest_flow_dt_g, nodeGroup_unit_node_inertia_dt_gee,
        nodeGroup_slack_capacity_margin_d_g/_inertia_dt_g/_nonsync_dt_g/_reserve_dt_eeg
      * Costs: costs_dt_p, costs_discounted_d_p/_p_, annualized_costs_d_p
      * CO2/process: CO2__, CO2_d_g, process_co2_d_eee, process_reserve_average_d_eppe,
        process_reserve_upDown_node_dt_eppe
      * Other: reserve_prices_dt_ppg, dual_invest_unit/connection/node_d_e
  - Added _DISPATCH_MAPPING_FILENAMES frozenset (20 entries) to data_models.py — these are
    dispatch mapping and structural parquet files loaded separately by dispatch_mappings.py;
    from_dict() now silently excludes them from the unknown-key warning
  - Organized fields in TimeSeriesResults by category with section comments
  - Re-ran flextool-scenario-results end-to-end: zero from_dict() warnings, all dispatch
    and summary plots generated successfully
  - run_flextool.py with scenario network_all_tech passes end-to-end
