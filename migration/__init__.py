"""Tooling for the python-preprocessing migration.

Phase 0 scaffolding:
- mps_parity: structural MPS comparison (order-independent, value-exact)
- inventory_mathprog_derivations: extract every derived set/param from flextool.mod
- (later) dependency DAG + per-step task dispatch

Not part of the runtime FlexTool package — these are migration-only tools.
"""
