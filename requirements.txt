# FlexTool dependencies
# This file mirrors pyproject.toml dependencies + spinetoolbox for the
# traditional installation path. For new installations, prefer:
#   pip install -e .              (FlexTool GUI only)
#   pip install -e ".[toolbox]"   (FlexTool GUI + Spine Toolbox)

pandas
openpyxl
odfpy
spinedb-api
python-calamine
xlsxwriter
pyyaml
sv-ttk
highspy
matplotlib
spinetoolbox
