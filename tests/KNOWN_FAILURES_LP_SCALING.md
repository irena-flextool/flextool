# Known failures after LP-scaling branch (`lp-scaling-2026-04`)

This file tracks `tests/test_scenarios.py` failures that were **not**
regenerated by Agent 13b (see commit `Agent 13b: regenerate scenario
goldens for drift-only failures`). Each entry is a suspected real
regression or a drift that exceeds the strict 1e-9 relative
total-objective tolerance used to gate fixture regeneration.

## Classification criteria

A scenario is regenerated when total-objective relative drift is ≤ 1e-9
**or** when the drift is a documented accounting change from an earlier
commit on this branch (bucket A/B in the Agent 13b triage). Anything
else is parked here for follow-up investigation before merging to
`new-outputs`.

## Bucket C (0 entries — all cleared)

All previously-parked Bucket C entries have been resolved. History:

### `multi_fullYear_battery_nested_24h_invest_one_solve` — cleared by Agent 16

- **Bisect (Agent 16)**: culprit is `3f26391` "Agent 3c: two-tier
  vq_state_up / vq_state_down".
- **Classification**: **Case A** (intentional architectural change).
  Splitting state-balance slacks into primary + escape tiers changes
  the LP column structure, which in this rolling-horizon nested
  multi-solve scenario causes HiGHS to converge to a slightly
  different primal solution (commodity_cost 1693.80 → 1694.36,
  objective 2861.50 → 2862.68, relative Δ ≈ 4.1e-4). The upward
  slack penalty cost is unchanged (4.0613) and K_rel=1000 at the
  single active node (west, p2035) is non-binding (358 MWh ≪ 1000),
  so the drift is not caused by escape-tier spill but by the LP
  basis shifting when two new primary/escape columns are added per
  `(n, d, t)` state-balance slack.
- **Resolution**: goldens regenerated by Agent 16.
